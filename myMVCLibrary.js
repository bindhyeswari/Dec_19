/**
 *
 * MVC - A simple MVC library featuring Models, views, and controllers
 * Models are created by using a Model constructor
 * Views are created by using a View constructor
 *
 * */

// todo: Pending jsdocs

function MVC() {
    MVC.init();
}

MVC.init = function () {
    // traverse the DOM looking for marker/attribute data-view to map controllers to views
};

MVC.Controller = function (controllerName, fn) {
   // creates a controller and maps it out
};
